import React from "react";

import { Card } from "semantic-ui-react";

function VictoryCheck({ bingoBoxes }) {
  console.log(bingoBoxes);

  return <Card.Header>Vinnare</Card.Header>;
}

export default VictoryCheck;
